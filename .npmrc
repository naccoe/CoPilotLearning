# Supply chain security controls (Recipe 2.1)
# Block automatic execution of install scripts
ignore-scripts=true

# Require packages to be at least 7 days old before installing
min-release-age=7

# Enforce lockfile integrity
package-lock=true

